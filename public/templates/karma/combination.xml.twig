<manialinks>
{% for widget in widgets %}
    {% if widget in ['hide_all', 'hide_window'] %}
        {% for id in widgetMap[widget] %}
            <manialink id="{{ id }}"></manialink>
        {% endfor %}
    {% else %}
        {{ widgetMap[widget] }}
    {% endif %}
{% endfor %}
</manialinks>
